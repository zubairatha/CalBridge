# Smart Task Scheduling System Requirements

# Core dependencies
requests>=2.28.0
python-dateutil>=2.8.0

# Database
sqlite3  # Built into Python standard library

# Testing (optional)
unittest  # Built into Python standard library

# CLI (optional)
argparse  # Built into Python standard library

# Note: This system integrates with the existing CalBridge setup
# Make sure CalBridge is running on http://127.0.0.1:8765
# Make sure Ollama is running with llama3 model available
